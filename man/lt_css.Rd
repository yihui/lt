% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab.R
\name{lt_css}
\alias{lt_css}
\title{Attach Custom CSS}
\usage{
lt_css(x, ...)
}
\arguments{
\item{x}{An \code{lt_tbl} object.}

\item{...}{One or more character scalars: URLs (containing \verb{://} or
starting with \verb{//}) or paths to local \code{.css} files.}
}
\value{
The \code{lt_tbl} with the stylesheets recorded.
}
\description{
Add user-supplied stylesheets that render after the built-in CSS, so
rules can override the defaults. Each argument is either a URL
(emitted as a \verb{<link>} tag) or a local file path (read and inlined as
a \verb{<style>} block).
}
\examples{
\dontrun{
lt(head(mtcars[, 1:3])) |> lt_css("custom.css")
lt(head(mtcars[, 1:3])) |> lt_css("https://example.com/theme.css")
}
}
