% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{format.lt_tbl}
\alias{format.lt_tbl}
\title{Render an \code{lt_tbl} to HTML}
\usage{
\method{format}{lt_tbl}(x, fragment = TRUE, inline_assets = TRUE, assets = TRUE, ...)
}
\arguments{
\item{x}{An \code{lt_tbl} object.}

\item{fragment}{If \code{TRUE} (default), return an HTML fragment suitable for
embedding. If \code{FALSE}, wrap in a minimal \verb{<html><body>} document.}

\item{inline_assets}{If \code{TRUE} (default), inline the CSS/JS as text. If
\code{FALSE}, emit \verb{<link>} / \verb{<script src=...>} tags (assets must be served
alongside the HTML).}

\item{assets}{If \code{TRUE} (default), include the CSS+JS runtime. Pass
\code{FALSE} to emit only the spec block when the runtime is already on the
page.}

\item{...}{Reserved for future use.}
}
\value{
A character scalar containing HTML.
}
\description{
Emits the CSS+JS runtime and a script block carrying the table's JSON spec.
Multiple tables on the same page only need the runtime once.
}
\examples{
tbl = lt(head(mtcars))
html = format(tbl)
format(tbl, fragment = FALSE)
}
