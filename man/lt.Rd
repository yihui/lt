% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lt.R
\name{lt}
\alias{lt}
\alias{lt.default}
\title{Create a Table Specification}
\usage{
lt(data, ...)

\method{lt}{default}(data, auto_format = TRUE, auto_label = TRUE, ...)
}
\arguments{
\item{data}{A data frame (or anything coercible to one).}

\item{...}{Arguments passed to methods.}

\item{auto_format}{Whether to automatically format numeric columns (rounding,
thousand separators, percentage detection). Set to \code{FALSE} to disable
for the whole table; use \code{\link[=lt_format]{lt_format()}} on specific columns to disable
selectively.}

\item{auto_label}{Whether to automatically clean column names for display
by replacing separators (\code{.} and \verb{_}) with spaces. Set to \code{FALSE} to
show raw column names.}
}
\value{
A table object that can be piped into \verb{lt_*()} functions.
}
\description{
Entry point of the lightweight grammar of tables. Returns an object (a
list) that records the data plus a list of table-modifying operations. The
object is rendered to HTML by \code{\link[=format]{format()}} (called automatically by the
print method).
}
\section{HTML in cells}{

By default all cell values and text (titles, labels, footnotes, ...) are
HTML-escaped, so special characters like \code{<}, \code{>}, and \code{&} render as
literal text. To emit content as raw HTML instead:
\itemize{
\item For body cells, mark whole columns with \code{\link[=lt_html]{lt_html()}}.
\item For a title, subtitle, column label, spanner, footnote, or note, wrap the
text in \code{\link[=I]{I()}}. For example, \code{lt_header(x, I("<b>Report</b>"))} renders the
title as raw HTML, while \code{lt_header(x, "<b>Report</b>")} shows the literal
\verb{<b>} tags.
}
}

\section{Interactivity}{

When a cell value has been formatted (e.g., by auto-formatting or
\code{\link[=lt_format]{lt_format()}}), the original value is stored in the cell's \code{title}
attribute and shown as a tooltip on hover. Additionally:
\itemize{
\item \code{Alt + Click} on a table toggles display of all raw values in that table.
\item \code{Alt + Double-Click} on a table toggles raw values globally (all tables
on the page).
}

Raw values are shown as \code{(value)} after the formatted text, highlighted
with a light yellow background.
}

\examples{
lt(head(mtcars[, 1:4]))
}
