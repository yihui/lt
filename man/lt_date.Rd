% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab.R
\name{lt_date}
\alias{lt_date}
\title{Format Date/Time Columns}
\usage{
lt_date(x, columns, method = NULL, locale = NULL, options = NULL)
}
\arguments{
\item{x}{An \code{\link[=lt]{lt()}} object.}

\item{columns}{Columns to select: a character vector of column names,
integer positions, or a one-sided formula.}

\item{method}{A JS Date method name to call (e.g., \code{"toLocaleDateString"},
\code{"toISOString"}, \code{"toDateString"}, \code{"toLocaleString"},
\code{"toLocaleTimeString"}). If \code{NULL} (the default),
\code{toLocaleDateString()} is used with \code{locale} and \code{options}. See
\url{https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date#instance_methods}
for all available methods.}

\item{locale}{A BCP 47 locale string (e.g., \code{"en-US"}, \code{"de-DE"},
\code{"ja-JP"}). Only used when \code{method} is \code{NULL}. See
\url{https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Intl#locales_argument}
for details.}

\item{options}{A named list of
\href{https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Intl/DateTimeFormat/DateTimeFormat#options}{\code{Intl.DateTimeFormat}}
options. Common fields include \code{year} (\code{"numeric"}, \code{"2-digit"}), \code{month}
(\code{"numeric"}, \code{"2-digit"}, \code{"long"}, \code{"short"}, \code{"narrow"}), \code{day}
(\code{"numeric"}, \code{"2-digit"}), \code{hour}, \code{minute}, \code{second} (\code{"numeric"},
\code{"2-digit"}), \code{weekday} (\code{"long"}, \code{"short"}, \code{"narrow"}), and
\code{timeZoneName} (\code{"long"}, \code{"short"}). Only used when \code{method} is \code{NULL}.}
}
\value{
\code{x} with the date formatting recorded.
}
\description{
Format date or datetime columns using JavaScript's native Date methods.
The underlying data should be Date or POSIXt in R (serialized as
\verb{new Date(...)} for the browser).
}
\note{
The formatted date may differ from the input date depending on the
viewer's local timezone. JavaScript's \verb{new Date("2024-01-15")} parses
date-only strings as UTC midnight, but \code{toLocaleDateString()} converts to
the local timezone. For example, \code{2024-01-15} will display as
\code{2024-01-14} for a viewer at GMT-6. To avoid this, pass
\code{options = list(timeZone = "UTC")}.
}
\examples{
d = data.frame(
  event = c("Launch", "Update"),
  date = as.Date(c("2024-01-15", "2024-06-30"))
)
lt(d) |> lt_date(~ date)
lt(d) |> lt_date(~ date, options = list(year = "numeric", month = "short"))
}
