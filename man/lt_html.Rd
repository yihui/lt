% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab.R
\name{lt_html}
\alias{lt_html}
\title{Render Column Cells as Raw HTML}
\usage{
lt_html(x, columns)
}
\arguments{
\item{x}{An \code{\link[=lt]{lt()}} object.}

\item{columns}{Columns whose cells are raw HTML: character names, integer
positions, or a one-sided formula. If missing, all columns are treated as
raw HTML.}
}
\value{
\code{x} with the raw-HTML columns recorded.
}
\description{
Mark whole columns so their body cells are emitted as raw HTML instead of
being escaped. By default every cell is HTML-escaped (so \code{<}, \code{>}, and \code{&}
render as literal text); use this to embed links, emphasis, or other markup
in a column's cells.
}
\details{
To emit raw HTML in other regions (title, column labels, footnotes, ...),
wrap the text in \code{\link[=I]{I()}} in the corresponding \verb{lt_*()} function instead.
}
\examples{
d = data.frame(
  name = c("<a href='#a'>A</a>", "<a href='#b'>B</a>"), n = 1:2
)
lt(d) |> lt_html(~ name)
}
